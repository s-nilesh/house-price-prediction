name: CI/CD Pipeline

on:
  pull_request:
    branches: [main]
  push:
    branches: [dev, main]

jobs:
  lint_format:
    if: github.event_name == 'push'
    uses: ./.github/workflows/lint_format_check.yml

  test:
    needs: [lint_format]
    if: always()
    uses: ./.github/workflows/test.yml

  # build:
  #   needs: test
  #   if: success()
  #   uses: ./.github/workflows/build-docker.yml

  # publish:
  #   needs: build
  #   if: success()
  #   uses: ./.github/workflows/publish.yml